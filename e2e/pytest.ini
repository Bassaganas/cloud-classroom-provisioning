[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    e2e: mark test as an e2e test
    slow: slow test
    instance: instance management test
    admin: admin instance test
    session: tutorial session test
    landing: landing page test
