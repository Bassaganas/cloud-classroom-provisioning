// Always-on environment links for tutorials (local dev fallback)
// Sync with AWS SSM Parameter Store value
export const alwaysOnLinks = {
  fellowship: [
    {
      label: "LOTR SUT",
      link: "https://lotr.testingfantasy.com/"
    }
  ]
};
